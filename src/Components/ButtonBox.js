import React from "react";

const ButtonBox = () => {
  return (
    <div style={{ textAlign: "center" }}>
      <button>THEME</button>
      <button>Change!</button>
      <button>Copy!</button>
    </div>
  );
};

export default ButtonBox;
